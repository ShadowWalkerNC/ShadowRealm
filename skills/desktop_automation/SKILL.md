# PC & Desktop Automation Skill

## Description
Guidelines for executing CLI tools, launching external applications, and inspecting system state.

## Instructions
1. Use `cli.py` or native shell execution for host applications (`docker`, `tailscale`, `gh`, `code`, `python`).
2. Provide concise output feedback to chat user.
